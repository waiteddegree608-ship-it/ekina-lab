# 伊姬娜实验室

这里是总管的账本，不是施工现场。
- `academic/` 论文、实验、claim 与负结果
- `projects/` 产品与工作流（AI 漫剧等）
- `ledger.jsonl` 时间序追加记录

开新稿用 `openNewPaper`（不需要现成 Overleaf 论文）。
推到 GitHub 用 `syncLabGithub`。
Overleaf 不能用 token 建项目：给一个空白写稿桌 Git 地址后，`syncOverleaf` 自动 push。
伊姬娜只派活；CODER 动手；记完再入库。
